spring:
  application:
    name: loan-service
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=microfinance2;encrypt=true;trustServerCertificate=true
    username: sa
    password: Mk@mw@ki071
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key-spec: "3fec0a961e99773a49912bd847eabaabfff4150f9054ad16de38aafbd5c6cc1b"
          jws-algorithms: HS256
  jackson:
    deserialization:
      accept-single-value-as-array: true
server:
  port: 8082

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.springframework.web.filter.CommonsRequestLoggingFilter: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

eureka:
  instance:
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}
    hostname: ${spring.cloud.client.ip-address}






